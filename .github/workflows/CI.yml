name: CI

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: xcodebuild -scheme ZippyZapTests -project ./ZippyZap.xcodeproj clean build test
      env:
        TEST_SCHEME: "ZippyZapTests"
